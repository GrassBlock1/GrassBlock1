name: Push to Gist

on:
  push:
    branches:
      - master
  schedule:
    - cron: 42 */4 * * * # 每两小时执行一次

jobs:
  push:
    runs-on: ubuntu-22.04
    env:
      FILE_NAME: 🌧 Quote
      GIST_ID: 42dcc0eeddd9e4595012c245391c51a0
    steps:
    - name: Pull
      run:
        |
        curl -sSL "https://gb0.dev/g/quotes?gist" -o quote.txt
    - name: Push
      uses: exuanbo/actions-deploy-gist@v1.1.4
      with:
        token: ${{ secrets.GISTS_TOKEN }}
        gist_id: ${{ env.GIST_ID }}
        gist_file_name: ${{ env.FILE_NAME }}
        file_path: quote.txt
        file_type: text